import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'SaaS Medical Tracker',
  description: 'Track medications, log symptoms, and manage your health data securely.',
  keywords: 'medical tracker, medication management, health tracking, symptoms log',
  authors: [{ name: 'SaaS Medical Tracker Team' }],
  creator: 'SaaS Medical Tracker',
  publisher: 'SaaS Medical Tracker',
  robots: {
    index: false,
    follow: false,
    googleBot: {
      index: false,
      follow: false,
    },
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <div className="min-h-screen bg-background font-sans antialiased">
          {children}
        </div>
      </body>
    </html>
  )
}